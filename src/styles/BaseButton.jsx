import { Button, styled } from '@mui/material';

const BaseButton = styled(Button)({
  borderRadius: '20px',
  paddingTop: '10px',
  paddingLeft: '60px',
  paddingBottom: '10px',
  paddingRight: '60px',
  margin: '10px',
});

export default BaseButton;
